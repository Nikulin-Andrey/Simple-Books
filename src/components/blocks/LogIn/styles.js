import { styled } from '@mui/material/styles'
import {
  CardMedia,
  Typography,
  IconButton,
  CardActions,
  Stack,
  Chip,
} from '@mui/material'

export const Title = styled(Typography)(({ theme }) => ({}))
